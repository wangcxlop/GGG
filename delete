# GWR/MGWR、多源卫星降水融合与分层验证研究计划

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: plan
- Origin Date: 2026-08-03
- Verification Status: UNVERIFIED（实验设计已结合仓库现状，新增实验尚未运行）
- Version: full_year_experiment_plan_v1

## 1. 研究目标与当前证据

目标是建立完整证据链，回答四个问题：

1. GWR是否比IDW、ADW、TPS等传统插值方法有效？
2. 哪些变量有效，哪些应作为local variable或global variable，GWR、Mixed GWR还是MGWR更合适？
3. FY-4B、GPM、GSMaP哪个质量更好，单卫星还是多卫星融合更好？
4. 改进是否在小雨、中雨、大雨、不同季节和不同地形区域中稳定，并具备后续洪水模拟价值？

当前基线来自 [技术说明](C:/Users/qw123/G/MGWR.jl/mger_mainflow_method.typ:1004) 和 [主结果表](C:/Users/qw123/G/MGWR.jl/output/mger_three_products_timealigned_5fold_loocv/summary_three_products.csv)：

- 随机站点五折下，RMSE分别为：FY-4B `1.658→1.057`、GPM `1.238→1.072`、GSMaP `1.215→1.034 mm/h`。
- 当前原始产品中GSMaP的RMSE/MAE较好，GPM的相关性较高，尚不能认定唯一最佳产品。
- 10 km缓冲后GPM和GSMaP已略有恶化；连续空间块五折后三产品RMSE均增至约5 mm/h。
- 因此现有证据只能证明“站网内部、同刻雨量站辅助的空间插值校正”，不能证明远距离无站区外推。
- 当前主模型只是经纬度驱动、所有变量共用一个带宽的残差GWR；仓库中的`MGWR`实际是Mixed GWR结构，不是经典multiscale GWR。
- NDVI、多卫星融合、全年季节评价和洪水模型尚未进入主流程。

## 2. 数据体系与变量方案

### 2.1 数据扩展和统一口径

- 将地面站、FY-4B、GPM、GSMaP扩展为2022–2024完整全年小时数据；2025年只作为将来的独立测试年，不在缺少同期站点观测时使用。
- 保留FY-4B严格四个15分钟片段聚合、导航审计以及GPM/GSMaP时间平移规则。
- 三产品采用完全一致的站点、时间、外层fold和评价掩膜；同时单独报告各产品覆盖率，避免共同样本筛选掩盖缺测差异。
- 比较阶段仍以站点处原始产品值进行，避免把不同卫星强行升采样后产生虚假空间信息。

### 2.2 预注册变量集

| 方案 | 变量 | 用途 |
|---|---|---|
| V0a | 局地截距 | 标准局地加权残差均值 |
| V0b | 截距＋中心化经纬度 | 精确复现当前模型 |
| V1 | V0a＋`log1p(卫星背景降水)` | 检验误差是否随降水强度变化 |
| V2 | V1＋高程、坡度、局地起伏、东西/南北梯度、`sin/cos(坡向)` | 检验地形增益 |
| V3 | V2＋滞后NDVI | 检验植被状态的独立增益 |
| V4 | 三卫星`log1p`降水＋通过筛选的地形和NDVI变量 | 多卫星直接融合 |

NDVI固定采用质量控制后的MOD13A2 V6.1、1 km、16日产品；该产品由NASA LP DAAC提供，并可通过Earth Engine读取。[官方数据说明](https://developers.google.com/earth-engine/datasets/catalog/MODIS_061_MOD13A2)

处理规则：

- 只能使用预测时刻之前已经完整发布的16日合成，禁止使用包含未来日期的合成值。
- QA不合格值设为缺失；最多向前保持32天，超过则该变量缺失。
- NDVI先做站点/像元尺度提取，不插值成虚假高分辨率。
- DEM统一使用Copernicus GLO-30；分别计算1、5、10 km邻域统计。
- 坡向不得直接作为0–360°连续变量，必须转换为正弦和余弦。
- 高程尺度通过内层CV选择；高度相关变量若`|r|>0.8`，保留物理解释更明确且CV更优者。
- 标准化、共线性筛选和尺度选择全部只在训练折完成。

### 2.3 local/global变量判定

不预先主观指定变量角色，而在每个训练折内完成：

1. 真MGWR为每个变量选择独立带宽。
2. 若变量带宽覆盖不少于90%训练站，且Monte Carlo空间变异检验经BH校正后`p≥0.05`，判为global。
3. 其余判为local。
4. 同一变量只有在至少4/5空间折中的角色一致时，才进入最终Mixed GWR；否则保持MGWR多尺度表达并标记为“角色不稳定”。
5. `global`表示该小时内系数在空间上恒定，`local`表示空间变化；二者都允许系数随小时重新估计。
6. 季节、月份等纯时间变量在单小时空间回归中与截距共线，不作为回归变量，只用于分层评价或分季节选择带宽。

## 3. 实验矩阵与验证设计

### 3.1 方法层级

所有方法使用完全相同的外层测试样本。

| 层级 | 方法 |
|---|---|
| 原始基线 | FY-4B、GPM、GSMaP原始值 |
| 纯站点插值 | IDW、ADW、TPS、GWR |
| 简单卫星订正 | 全局均值偏差订正 |
| 卫星残差插值 | Residual-IDW、Residual-ADW、Residual-TPS |
| 空间回归 | 当前Residual-GWR、Mixed GWR、真正的multiscale GWR |
| 多卫星基线 | 三产品均值、中位数、训练折内非负且和为1的全局权重 |
| 多卫星空间融合 | V4下的GWR、Mixed GWR、MGWR |

传统方法同时运行“纯站点插值”和“卫星残差插值”，从而区分增益来自卫星背景还是空间插值器。

参数全部在内层CV选择：

- IDW：幂指数`1, 1.5, 2, 2.5, 3`；邻居数`8, 16, 32, 64, all`。
- ADW：使用IDW距离权重乘角度分布修正项；与IDW使用相同参数网格。
- TPS：在以训练站中心建立的等距公里坐标中拟合，平滑参数通过训练折GCV选择。
- GWR系列：比较Bisquare、Gaussian、Exponential核；固定带宽`10, 20, 30, 50, 80, 120 km`，自适应带宽`30, 50, 80, 120, 160, 200`邻居。
- MGWR采用变量独立带宽回代，收敛阈值`1e-5`、最多200次；不收敛时该fold记失败，不回退并伪装成MGWR结果。

### 3.2 分阶段运行，避免组合爆炸

1. **原始产品评价**：在三产品共同样本上做配对排名，同时报告各自完整覆盖率。
2. **方法评价**：V0a/V0b下完整比较IDW、ADW、TPS、GWR及其残差版本。
3. **变量消融**：对GWR、Mixed GWR、MGWR依次比较`V0→V1→V2→V3`。
4. **单卫星比较**：每种通过变量门控的模型分别作用于FY-4B、GPM、GSMaP。
5. **多卫星融合**：比较均值、中位数、全局权重和空间变权重，并与最佳单卫星配对比较。
6. **分层稳健性**：固定最终候选，不再调参，评价雨强、季节、区域和空间距离。
7. **最终产品生成**：仅在全部验证完成后，使用全站网重新拟合，生成0.1°小时降水及事件流域平均输入；OOF结果不得冒充最终业务产品。

### 3.3 外层验证

采用三套互补验证，结论边界分别报告：

- **主验证：3年×5空间折**。依次留出2022、2023、2024作为时间外层测试年；每个测试年内再执行共享的空间分层五折。历史年份只用于变量、融合权重和超参数选择；测试年同刻允许使用其他训练站，符合站网辅助实时场景。
- **站网内部插值**：空间分层时兼顾区域和高程等级，证明密集站网中的插值能力。
- **严格空间外推压力测试**：二维空间块五折并删除验证块边界10 km内训练站。若失败，必须保留负结果，不得声称适用于远距离无站区。
- 所有卫星、方法和变量方案共享相同fold。
- 修改验证站观测不得改变预测、变量选择、带宽或融合权重。

## 4. 分层评价与判定规则

### 4.1 雨强、季节和区域

雨强按地面观测小时雨量固定划分：

- 小雨：`0.1–2.5 mm/h`
- 中雨：`2.5–8 mm/h`
- 大雨：`≥8 mm/h`
- `≥16 mm/h`作为极端降雨敏感性结果

不把无雨作为效果分层；但无雨样本仍进入FAR和虚警审计，防止通过普遍报雨人为提高POD。

季节按DJF、MAM、JJA、SON划分。每个分层至少需要30个独立降水事件和30个站点，否则只做描述性结果，不做显著性结论。

区域由统一DEM规则定义：

- 平原：高程 `<200 m`且10 km起伏 `<100 m`
- 丘陵：不满足平原或山区条件
- 山区：高程 `≥500 m`或10 km起伏 `≥300 m`

若某类少于30个站点，按固定规则与相邻地形等级合并，并在运行清单记录。

### 4.2 指标与统计单位

- 连续指标：RMSE、MAE、Bias、相关系数、KGE式降水一致性指标。
- 事件指标：POD、FAR、CSI；阈值为0.1、2.5、8和16 mm/h。
- 大雨补充：事件累计量误差、峰值强度误差、峰值时刻误差、95/99分位数误差。
- 统计推断以“站点—降水事件”为单位，不把数百万小时样本当作完全独立观测。
- 使用事件与空间块聚类bootstrap 2,000次计算95%区间；多模型比较采用Holm校正。

### 4.3 预注册决策门槛

| 要证明的结论 | 通过条件 |
|---|---|
| GWR有效 | 大雨RMSE比最佳IDW/ADW/TPS至少降低5%，bootstrap区间不跨0；中雨RMSE不恶化超过2%；至少2/3年份和2/3区域方向一致 |
| 新变量有效 | 加入后大雨或中雨RMSE至少改善2%且区间不跨0；其他主要指标无超过5%的系统恶化 |
| 选择Mixed GWR | local/global角色至少4/5折稳定，且优于全局模型和全局带宽GWR |
| 选择MGWR | 大雨RMSE比GWR和Mixed GWR均至少改善2%，区间不跨0，带宽和收敛状态稳定；否则选择更简单模型 |
| 最佳卫星产品 | 在共同样本上配对优于其他产品，并同时满足雨强、季节、区域稳定性；否则报告“指标依赖型排名” |
| 采用多卫星融合 | 比最佳单卫星的大雨RMSE至少改善2%，且任何季节或区域不恶化超过5%；否则保留最佳单卫星 |
| 支持空间外推 | 严格空间块+10 km缓冲仍优于原始卫星和最佳传统方法；未通过时只保留站网内部结论 |

模型选择采用上述门槛和简约原则，不以单个最小RMSE直接决定。

## 5. 实现接口、输出与测试

新增统一实验入口：

```text
julia --project=. scripts/run_experiment_matrix.jl \
  --config configs/full_year_benchmark.toml
```

配置公开字段至少包括：

```text
products
background_mode
method
variable_set
variable_roles
kernel
bandwidth_candidates
cv_scheme
rain_bins
seasons
terrain_regions
random_seed
```

方法标识固定为：

```text
raw
mean_bias
idw
adw
tps
residual_idw
residual_adw
residual_tps
gwr
mixed_gwr
multiscale_gwr
```

现有`MGWR`结构不得继续代表真正多尺度模型；输出和文稿统一区分`mixed_gwr`与`multiscale_gwr`。

规范输出目录为`output/mger_full_year_benchmark/`，至少包含：

- `data_audit.csv`：输入版本、时间范围、站点、覆盖率、哈希和时间对齐。
- `experiment_registry.csv`：全部方法、变量、产品、参数、fold和运行状态。
- `oof_predictions.csv`：唯一允许用于正式评价的外层OOF预测。
- `metrics_stratified.csv`：雨强、季节、区域、年份和空间距离结果。
- `paired_comparisons.csv`：效应量、bootstrap区间、多重比较校正结果。
- `variable_roles_bandwidths.csv`：每折local/global判定及MGWR带宽。
- `fusion_weights.csv`：全局/局地卫星权重及稳定性。
- `model_decision.json`：按预注册规则给出的最终选择及失败门控。
- `limitations.md`：随机站网、空间块、缺测和业务适用范围。
- `hydrology_ready_event_rainfall.csv`与0.1°小时栅格：只作为后续水文输入，不报告洪水提升。

必须自动测试：

- 三卫星和所有方法的fold完全一致。
- 验证站观测极端扰动不改变预测与调参。
- 标准化、变量筛选、NDVI填补、融合权重只使用训练数据。
- 校正前后使用同一有效样本掩膜，并输出`pre_n/post_n/common_n/coverage`。
- NDVI不存在未来信息；坡向转换和DEM尺度计算正确。
- IDW/ADW/TPS在人工空间场上满足已知结果。
- 合成多尺度数据能恢复不同变量带宽，证明真正MGWR不是Mixed GWR重命名。
- 多卫星全局权重非负且和为1。
- 大雨样本不足、MGWR不收敛或空间块失败时明确标记，不静默回退。
- 无雨不生成独立效果分层，但FAR仍可复算。

## 6. 洪水预报延伸边界

本阶段按用户选择，不做洪水精度定量结论，只完成“水文可接入”产物。

进入下一阶段必须同时满足：

1. 中雨和大雨订正通过上述门槛；
2. 有独立流域边界、小时流量/水位、产汇流模型及率定期；
3. 水文参数只用雨量站基准降水率定，替换卫星输入时不得重新率定；
4. 至少比较原卫星、最佳单卫星订正、最佳多卫星融合和站点基准；
5. 使用独立洪水事件评价洪峰误差、洪量误差、峰现时间、NSE、KGE和洪水命中率；
6. 只有降水改善能够稳定传递到洪峰/洪量改善，才写“提高洪水预报精度”。

最终论文主线应表述为：先证明原始产品质量差异，再证明传统方法与GWR家族的相对增益，再用变量消融判定local/global与MGWR必要性，随后检验单/多卫星融合，最后用雨强—季节—区域—空间距离四维结果限定适用范围；洪水提升暂作为通过降水门控后的独立后续研究。






"F:\G\GeoWeightedRegression.jl\reference_code"这是我在另一个项目里面的成熟代码文件， 你看看和我们的scr/ 和 script/ 相比，把可以加以保留和替换的告诉我