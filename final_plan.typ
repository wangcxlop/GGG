= 现在的研究方案和目标
现在是将站点随机分为五份,其中80%作为训练集,20%作为验证集,对每份训练集进行loovc调参求解得出针对每一种核函数的最佳带宽, 使用五种核函数及其对应的带宽应用到验证站上得到输出的五种验证结果,这样每一折都会得出五种核函数对其验证站的预测结果

再利用out-of-old数据,结合每一折的各个核函数的预测结果各自拼接成全体站点的预测数据,使用RMSE、MAE、Bias、相关系数和POD/FAR/CSI等评价指标得到最优的卫星产品和核函数及带宽等等参数


== 现在目标





优化模型值mgwr ,参考#image("assets/image-32.png")
//采用 gwr? 模型和 IDW,TPS,AD都进行比较



消除验证集赌博现象,作为对采用固定随机种子的方法优化




//将传统方法改为直接插值，取消输入残差的方法


加入DEM、ERA5等数据进行筛选后进行运算













== 论文中得到的思路点



《基于混合地理加权回归模型的华北地区降水量空间插值研究》 中该研究与gwr与三种传统插值方法的比较关于如何实现提供了思路

关于如何构建模型及相关表述
#image("assets/image-15.png")

描述了带宽选择的方法
#image("assets/image-16.png")
#image("assets/image-17.png")


降水融合的方法描述
#image("assets/image-18.png")




将卫星观测与地面观测融合
#image("assets/image-19.png")

关于几种融合方法的表述及方法
#image("assets/image-20.png")
#image("assets/image-21.png")
#image("assets/image-22.png")
#image("assets/image-23.png")


MGWR与传统方法的描述
#image("assets/image-24.png")
#image("assets/image-30.png")

关于我们的变量选择与其合理性检测描述
#image("assets/image-25.png")
#image("assets/image-29.png")





= kdd的问题
#image("assets/image-26.png")


关于我们的变量选择是否有效:先选出一些变量,然后对每个卫星产品进行再检测(做相关性检测)选择其合理的变量,筛选出一部分作为解释变量加入我们的研究


关于是 GWR 还是 MGWR :我们做 MGWR


融合一个卫星、还是多个卫星(local variable,global variable):没有融合


不同时段的效果（小雨、中雨、大雨），不同季节、不同区域，以及洪水预报精度提升:











#image("assets/image-27.png")


关于方法方面:MGWR与三种传统插值方法的比较
关于数据方面:对变量做相关性检验来筛选
效果证明方面:
关于不同季节:可以做
关于不同区域:可以做

= 解决的问题
后面问ai

解决了在十堰区域的卫星降水与观测问题,提升洪水预报


= 大致的方法框架


使用五折交叉验证,将站点分为五份,五次中轮流有一份作为验证集,其余四份作为训练集\
同时再每折内进行LOOCV调参得到最优参数如带宽等参数

对gwr残差模型(使用五种核函数和其对应的最优参数),我们使用训练集内部卫星观测数据与地面站点的观测数据的差,作为残差输入,得到验证集的预测残差,再加回验证站的卫星降水,得到该折验证站的校正降水,重复五次即得到全部站点的校正降水,最终得出三种卫星产品五种核函数的校正降水数据

对三种传统插值模型,同样采用五折交叉验证,仅采用直接插值方式:
+ IDW,ADW,TPS直接插值,输入训练站观测降水,输出验证站预测降水
+ 不再采用IDW,ADW,TPS残差插值；残差校正仅用于GWR主模型

随后进行计算评价指标,得出最优卫星产品和最佳方法





































#image("assets/image-28.png")










= GWR残差校正模型

训练站中的卫星观测降水$P_(s a t)$减去地面站点的观测降水$P_("obs")$得到训练站的残差$R$\
_Tip_:由于目前的变量仅采用空间距离的变化,是gwr


先计算对某一验证站点$j$和到及其相关训练站点$i$的空间距离$d_(i, j)$\
由于前期训练站内已通过LOOCV调参得到针对各个核函数的最优带宽,因此对同时刻的需要预测的验证站的预测残差,我们输入对应的空间距离$d_(i, j)$和最优带宽通过核函数运算得出预测站点$i$对验证站点$j$的空间权重\
得到各训练站点的的空间权重组合形成对验证站点$j$的空间权重矩阵,进入加权回归(使用加权最小二乘回归？或者还是什么相关的加权回归公式？)得到局部系数\
得到局部系数后使用局部回归方程获得该验证站的预测残差$hat(r)$\
将 $hat(r)$ 加回 卫星在该验证站的观测值，得到校正降水


















== 目标
+ 补充卫星观测数据FY4B,GPM,GsMAP为2022-2025全年数据
+ 加入高程等自变量
+ 思考若采用MGWR ,或 Mixed GWR ,我们的自变量该如何选择得到最优,如何证明我选择的变量有效(局部变量,全局变量)
+ 根据我们的区域不同研究对效果验证方面的影响
+ 小雨中雨大雨的不同卫星融合效果怎么样



= 三种模型演变

#image("assets/image-14.png")







== 其他融合可用方法


= 计算残差
$
  R_i = P_(o b s, i)-P_(s a t, i)
$
== 估计其他位置上的残差
根据已知雨量站位置上面的残差$R_i$, 估计没有雨量站位置上的残差 $hat(R(x,y))$就是我们后面的核心任务

这一步的方法很多,主要有一下几种


#table(
  columns:(auto, auto, 1fr),
  align:left,
  inset:8pt,
  stroke:0.5pt,

  table.header(
    [*方法类别*],
    [*常见方法*],
    [*核心思想*],
  ),
    [*空间插值方法*],
  [IDW、Kriging、Spline、RBF],
  [根据周围站点残差推算未知位置残差],

  [*空间回归方法*],
  [GWR、MGWR、空间回归、GAM],
  [用位置和环境变量解释残差空间变化],

  [*时空统计方法*],
  [GTWR、时空 Kriging、ST-GWR],
  [同时考虑空间距离和时间距离],

  [*机器学习方法*],
  [RF、XGBoost、LightGBM、SVR],
  [学习残差与卫星、地形、气象变量之间的非线性关系],

  [*深度学习方法*],
  [MLP、LSTM、CNN、ConvLSTM、Transformer],
  [学习复杂空间、时间和非线性结构],

  [*混合方法*],
  [RF+Kriging、GWR+ML、CNN+LSTM],
  [把统计模型、插值和机器学习组合起来],






)

























= 目前流程及方法公式

#image("assets/image-13.png")
== 计算空间距离
这里我们使用的是 Haversine 球面距离

+ 先从站点元数据提取 lon、 lat,得到经纬度坐标矩阵
+ 使用 $"Haversine"(6378.388)$
+ 计算公式等价于
$
  d_(i, j) = 2 R arcsin (sin^2(Delta phi) / 2 + cos phi_i cos phi_j sin^2(Delta lambda / 2))^(1/2)
$
=== 输入与输出

+ 站点元数据的lon、lat
- $d_(i, j)$


== 计算空间权重


空间权重基本形式
$
  w_(i, j) = K(d_(i, j) / (b w_j))
$

_tip_:
- $d_(i, j)$:地i给训练站点到第j个目标站点的空间距离
- $b w_(i,j)$:目标站点j的带宽
- K(.):核函数
- $w_(i, j)$:训练站点i对目标站点j的空间权重


目前的主流程使用的是 BISQUARE 核函数




=== 输入及输出
+ $d_(i, j)$
+ 带宽 $b w_j$


- 空间权重$w_(i, j)$
 


== 带宽选择与确定
直接定义候选带宽为以下且核函数固定为 BISQUARE:
- 自适应带宽: 50、80、120、150、 200
- 固定带宽:10、20、30、50 KM

对每个候选组合,流程为:
+ 用该带宽结合核函数等计算空间权重
+ 根据训练站点的观测值和卫星值进行偏差校正
+ 计算校正后的指标RMSE, MAE, Bias、相关系数等指标
+ 计算AICc,若不适合,则改用 GVC
+ 按 RMSE 从小到大排序,若RMSE相同,再按 AICC 排序
+ 选择排第一的组合






== 组合的空间权重矩阵

代码中为隐式, 只保留了一个整体的$w_(i, j)$


== 计算局部系数(PROBLEM)
_Tip_:i为目标位置,r为训练站点


+ 先计算残差
$
  R_(r, t) = T_(o b s,r, t) - Y_(s a t, r, t)
$
+ 构造局部回归设计矩阵为($l o n_c$$l a t_c$为训练站点的平均经纬度):
$
  X_r =mat(
    delim:"[",
     1 ,l o n_r - l o n_e, l a t_r - l a t_c
) 
$

模型实际拟合的是
$
R_(r,t)
=
beta_(0,i,t)
+ beta_("lon",i,t) ("lon"_r - "lon"_c)
+ beta_("lat",i,t) ("lat"_r - "lat"_c)
+ epsilon_(r,t)
$



对应的加权系数

$
hat(bold(beta))_(i,t)
=
(X^T W_i X + lambda I)^(-1)
X^T W_i R_t
$

+ $W_i = d i a g(w_(1i), w_(2i), dots, w_(n i))$

+ $w_(r i)$ 是训练站点 $r$ 对目标位置 $i$ 的空间权重；
+ $lambda = 10^(-8)$，用于缓解局部矩阵接近奇异的问题；
+ $bold(beta)_(i,t)$ 包括截距、经度系数和纬度系数









== 局部预测值



#image("assets/image-10.png")



== 五折交叉验证
主流程采用的是 外层五折站点交叉验证 + 每折训练集内部 LOOCV 调参

#image("assets/image-11.png")

#image("assets/image-12.png")
