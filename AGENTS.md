# AGENTS.md

## 项目说明

这是一个使用 Julia 编写的卫星降水校正项目，主要涉及：
- GWR / MGWR
- 时空地理加权回归
- FY4B、GPM、GSMaP 卫星降水
- 雨量站观测数据

## 编码要求

- 使用 Julia 语言。
- 不要无关重构。
- 不要随意修改已有函数名和参数。

## 修改原则

- 只修改与当前任务有关的代码。
- 如果发现其他问题，先指出，不要直接修改。
- 不要删除现有功能。
- 优先使用简单、易理解的实现方式。

## 代码风格
- 尽量简洁凝练， 可参照现存代码

## 项目目录规范
创建、修改或移动文件时，必须遵守以下目录结构。也不要在项目根目录随意创建临时脚本、数据文件、图片或输出结果。

### `assets/`
存放项目的静态资源
- 图片插图
- 项目示意图

### `data/`
存放研究数据
data/
├── raw/
└── processed/

其中：
- `data/raw/`：原始数据，原则上禁止修改
- `data/processed/`：经过清洗、转换、匹配或预处理的数据

### `output/`
存放程序运行生成的所有结果,禁止将生成结果写回 `data`


## `scripts/`
存放可以直接运行的任务脚本

-  `scripts/` 负责“调用”核心代码
- 核心算法不要直接写在脚本中


### `scr/`
存放项目核心 Julia 源代码

例如：
- 距离计算
- 空间权重
- 带宽选择
- 加权回归求解
- GWR / MGWR
- 精度评价工具


规则:
- 可复用的核心函数必须优先放入 `scr\`
- 不要在 `src/` 中写一次性实验流程
- 不要在 `src/` 中读取固定的本地绝对路径
- 核心函数尽量通过参数接收数据和配置


## 新文件放置规则
创建新文件时， 先判断其用途

- 程序运行结果 → `output/`
- 核心算法 → `src/`
- 可直接运行的实验或处理流程 → `scripts/`

`scripts/` 负责流程, `src/`负责可复用核心算法